# TODO

- [ ] **Unhide "Add to Calendar" button**
    - The "Add to Calendar" button is currently hidden (commented out) in `src/App.jsx`.
    - To unhide it, go to `src/App.jsx` around line 120 and remove the `{/*` and `*/}` comment markers around the `#calendar-button` element.
